[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "cilamp"
version = "0.2.0"
description = "Cloud Identity Lifecycle & Access Management Platform"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.41,<2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8,<10",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
addopts = "-q -p no:cacheprovider"
testpaths = ["tests"]
pythonpath = ["src"]
