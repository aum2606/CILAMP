[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "cilamp"
version = "0.8.0"
description = "IAM ConCen — AWS Identity & Access Management Command Center"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.40,<2",
    "streamlit>=1.41,<2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8,<10",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
addopts = "-q -p no:cacheprovider"
testpaths = ["tests"]
pythonpath = ["src"]
