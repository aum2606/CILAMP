# CILAMP Phase 0 uses simulation mode only.
CILAMP_MODE=SIMULATION

# Optional. Relative paths are resolved from the repository root.
CILAMP_DATABASE_PATH=data/cilamp.db

# Cloud settings intentionally do not appear until their approved phases.
# Never place real credentials in this example file or commit a local .env.
